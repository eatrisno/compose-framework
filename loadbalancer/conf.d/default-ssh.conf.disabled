server {
  listen 443 ssl default_server;
  server_name _;

  include /etc/nginx/conf.d/ssl.include;

  location / {
	return 404;
  }

  access_log off;
  error_log  /var/log/nginx/default-server.error.log error;
}